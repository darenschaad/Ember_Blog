<div class="postContainer">
  <h2>{{post.title}}</h2>
  <h4>{{post.author}}</h4>
  <img src={{post.image}} alt={{post.title}} />
  <!-- substring of post.body -->{{shortBody post.body}}
  <!-- {{#link-to 'post'}}    link to dynamic post {{/link-to}} -->
</div>
