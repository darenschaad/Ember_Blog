import { shortBody } from 'blog/helpers/short-body';
import { module, test } from 'qunit';

module('Unit | Helper | short body');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortBody([42]);
  assert.ok(result);
});
